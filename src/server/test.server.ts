import { Workspace } from "@rbxts/services";
import colorfool from "shared/packages/colorfool";
/*let pap = Workspace.FindFirstChild('pap') as BasePart;
let pip = Workspace.FindFirstChild('pip') as BasePart;

let p = new colorfool.effectors.pulse(pap);
p.render();

let p2 = new colorfool.effectors.pulse(pip);
p2.render();*/